// index.js is where connecting the React file to HTML file
import Reach from "react"
import ReactDom from "react-dom"

import App from "./App"

ReactDom.render(<App />, document.getElementById("root"));